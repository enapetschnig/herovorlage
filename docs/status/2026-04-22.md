# Status 2026-04-22 — Phase 0 + Start Phase 1

## Done
- Monorepo-Struktur (`apps/`, `packages/`, `docs/`) angelegt
- ADR-0001 (Supabase als Backend) + ADR-0002 (ULIDs + Soft-Delete) geschrieben
- `.env.local` mit Supabase-Credentials befüllt
- `packages/db`: Drizzle-Schema für alle Core-Tabellen aus CLAUDE.md Teil H.1 (+ ausgewählte Modul-Tabellen) implementiert
- `packages/schemas`: Zod-Schemas für Kontakte, Projekte, Dokumente, Artikel
- `packages/utils`: ULID-Helper, Currency-Formatter, Tenant-Scope-Wrapper
- `packages/auth`: Auth.js v5-Config mit Credentials-Provider + Drizzle-Adapter
- `packages/api`: tRPC-Router-Setup (Context, Middleware, Router für `tenant`, `contacts`, `projects`)
- `apps/web`: Next.js 15 App Router + Tailwind 4 + shadcn/ui-Grundkomponenten + Login + Dashboard + Kontakte-Liste + Projekt-Liste
- Schema in Supabase gepusht, Seed-Daten eingefügt (Demo-Tenant, Admin-User, Beispiel-Kontakte/Projekte)

## Next (Phase 1 Weiter)
- 5-Tab-Kontakt-Formular voll ausbauen (Tabs Kondition, Zahlungsdaten, ZUGFeRD)
- Kontakt-Detail-Seite mit Projekt-Liste, Dokumenten-Liste, Logbuch
- Projekt-Detail mit Tabs (Übersicht, Logbuch, Dokumente, Termine, Aufgaben, Zeit, Fotos)
- File-Upload zu Supabase Storage (Kontakt + Projekt)
- Globale Suche (Cmd+K) via Meilisearch (Docker lokal) oder Postgres trigram-Suche als Fallback
- Logbuch-System (logbook_entries) mit Rollen-Sichtbarkeit
- CSV-Kontakt-Import

## Open Questions
- Meilisearch lokal via Docker oder Supabase `pg_trgm`/`tsvector` für V1?
- Demo-Login-Credentials sollen in Prod natürlich entfernt werden — Onboarding-Flow für neue Tenants kommt in Phase 7
- Supabase Storage-Buckets (`contact-files`, `project-files`, `documents`) müssen per RLS abgesichert werden — kommt mit der File-Upload-Feature
