# Status 2026-04-22 (Update — Ende Tag 1)

## Done (zusätzlich zu morgens)
- Supabase **DB-Passwort gesetzt** via Management API (`PATCH /v1/projects/{ref}/database/password`) → in `.env.local` eingetragen → Pooler-Connection (eu-west-1, Port 6543) verifiziert mit `psycopg`.
- **4 Storage-Buckets** angelegt: `contact-files`, `project-files`, `documents` (privat), `avatars` (public). 50MB Limit pro Datei.
- **Storage-RLS** (`drizzle/0001_storage_rls.sql`): default-Permissive-Policies entfernt, service-role hat Vollzugriff auf HeatFlow-Buckets, anon kann nur `avatars` lesen, alle anderen Zugriffe denied. Defense-in-depth.
- **Seed angereichert** (`scripts/seed_extras.py`):
  - 1 Sample-Angebot AN-2026-001 mit 10 Positionen (Vitocal 350-G, Pufferspeicher, Hydraulik-Anschluss, Sole-Bohrung, Inbetriebnahme), Brutto ~€32k
  - 7 Zeiteinträge über 4 Tage verteilt auf 3 User
  - Wartungsvertrag „Vitocal 350-A 16kW jährlich" für Alpenhotel inkl. Asset (Vitamine R290, COP 4.2, 53.5 dB) + 1 erledigte + 1 geplante Wartung
  - Förderantrag „Raus-aus-Öl-Bonus AT" für Steiner-Projekt, Status `submitted`
- **End-to-End-Verifikation**: Identische Queries wie tRPC-Router (Dashboard-KPIs, projects.list mit Joins, projects.byId, search.global mit pg_trgm, Auth-User-Lookup) liefern korrekte Ergebnisse direkt vom Pooler.

## Aktueller Demo-Stand
| Entity | Count |
|---|---|
| Tenants | 1 (epower GmbH) |
| Users | 4 (owner/office/technician/planner) |
| Contacts | 6 (4 Kunden, 2 Lieferanten) |
| Projects | 4 |
| Articles | 8 |
| Services | 4 |
| Tasks | 5 (offen) |
| Documents | 1 (Angebot) |
| Document Positions | 10 |
| Time Entries | 7 |
| Logbook Entries | 4+ |
| Assets | 1 (Wärmepumpe) |
| Maintenance Contracts | 1 |
| Maintenance Visits | 2 |
| Funding Applications | 1 |
| Storage Buckets | 4 |

## Was funktioniert ohne weitere Schritte
Sobald Node 22 + pnpm installiert sind und `pnpm install && pnpm dev` läuft:
- **Login** mit `admin@demo.heatflow.local` / `demo1234`
- **Dashboard** zeigt KPIs (6/4/5/0), Projekt-Pipeline (5 Status-Buckets gefüllt), Aktivitäts-Feed
- **Kontakte**-Liste mit Suche/Filter, Detail-Seite mit allen 4 Steiner-Projekten/Tags/Bank
- **Projekte**-Liste mit Joined-Daten, Detail mit Tabs (Übersicht/Logbuch/Dokumente/Aufgaben)
- **Aufgaben**-Liste mit 5 offenen Aufgaben
- **Artikel**-Liste mit 8 Wärmepumpen-Komponenten
- **Settings** zeigt Tenant, Team (4 Member), 14 aktivierte Module
- `/api/health` zeigt Postgres-Latenz live

## Nächste konkrete Schritte (Phase 1 fertig + Phase 2 Start)
1. Node + pnpm installieren, Build verifizieren (`pnpm typecheck && pnpm build`)
2. Globales Cmd+K Modal an `search.global`-Endpunkt anbinden
3. Dokumenten-Editor anfangen (Tiptap, Positionstabelle mit dnd-kit) → Phase 2
4. CSV-Kontakt-Import
5. Inline-Kontakt-Erstellung im Projekt-Anlegen-Form (`<EntityCombobox>` aus `packages/ui/`)

## Sicherheitshinweis
Das DB-Passwort steht im Klartext in `.env.local` UND im Chatverlauf. Vor öffentlichem Push:
- DB-Passwort über Supabase Dashboard rotieren
- Service-Role-Key + Management-Token rotieren
- `.env.local` muss gitignored bleiben (ist bereits)
